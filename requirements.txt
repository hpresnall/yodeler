pyyaml
coverage
isort

# python3 -m venv .venv
# source .venv/bin/activate
# pip install --upgrade pip
# pip install -r requirements.txt
# coverage run --source=. -m unittest discover && coverage html
# python3 yodeler.py sites/standard /tmp
# or source ~/.local/pipx/venvs/coverage/bin/activate;python3 yodeler.py sites/standard /tmp;deactivate